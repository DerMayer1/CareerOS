# Workflow: Reset

Generated local data can be cleared with:

```bash
career-os reset --data
```

The explicit `--data` flag is required. This clears local generated data, reports, and tables. It does not edit `profile/`, `config/`, `skills/`, or workflow documentation.
